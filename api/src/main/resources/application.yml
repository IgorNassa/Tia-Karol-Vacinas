spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: tia-karol-api
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/tia_karol}
    username: ${DATABASE_USERNAME:tia_karol}
    password: ${DATABASE_PASSWORD:tia_karol}
    hikari:
      maximum-pool-size: ${DATABASE_MAX_POOL_SIZE:10}
      minimum-idle: ${DATABASE_MIN_IDLE:1}
      connection-timeout: ${DATABASE_CONNECTION_TIMEOUT_MS:30000}
  flyway:
    enabled: true
    locations: classpath:db/migration
    default-schema: app
    schemas: app
    create-schemas: true
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: app
    open-in-view: false

server:
  port: ${SERVER_PORT:8080}

springdoc:
  swagger-ui:
    path: /swagger-ui.html

supabase:
  url: ${SUPABASE_URL:}
  publishable-key: ${SUPABASE_PUBLISHABLE_KEY:}

bootstrap:
  admin:
    email: ${BOOTSTRAP_ADMIN_EMAIL:}
    password: ${BOOTSTRAP_ADMIN_PASSWORD:}
    name: ${BOOTSTRAP_ADMIN_NAME:Administrador}
